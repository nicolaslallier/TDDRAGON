version: '3.8'

# Production override for docker-compose.yml
# Use with: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up

services:
  # Example: Production overrides for endpoints
  # 
  # endpoint-example:
  #   environment:
  #     - ENV=production
  #     - LOG_LEVEL=WARNING
  #     - DEBUG=false
  #   restart: always
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: '1'
  #         memory: 512M
  #       reservations:
  #         cpus: '0.5'
  #         memory: 256M

