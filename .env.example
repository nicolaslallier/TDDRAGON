# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=tddragon
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Database URL (constructed automatically if not set)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tddragon

# Application Configuration
ENV=development
LOG_LEVEL=INFO

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
